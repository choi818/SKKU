#include <stdio.h>
#include <limits.h>
#include <float.h>

int main() {

    printf("float maximum: %e\n", FLT_MAX);
    printf("float minimum: %e\n", FLT_MIN);
    printf("double maximum: %e\n", DBL_MAX);
    printf("double minimum: %e\n", DBL_MIN);

    return 0;
}
